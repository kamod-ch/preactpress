import "@preactpress/plugin-playground/style.css";
/** Client-side entry — imports playground styles on pages that use the plugin. */
export function enhanceContent() {
  // Styles are imported statically above; no runtime work required.
}
export default enhanceContent;
//# sourceMappingURL=client.js.map
