---
title: useCounter
description: A minimal counter hook for demos and tests.
---

# useCounter

```tsx
import { useCounter } from "@example/hooks";

const { count, increment } = useCounter();
```

Returns the current count and an `increment` function.
